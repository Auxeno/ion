# Positional

Positional encodings for sequence models.

In the learned table's shape annotation, `m` is the configured maximum sequence
length. An input's actual sequence length is `s` and may be shorter than `m`.

::: ion.nn.RoPE

::: ion.nn.LearnedPositionalEmbedding

::: ion.nn.SinusoidalPositionalEmbedding
