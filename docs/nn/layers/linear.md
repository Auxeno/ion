# Linear

Fully connected layers. `Linear` applies `x @ w + b` over the last dimension and handles any number of leading batch dimensions; `Identity` is a no-op placeholder useful for optional layers and residual stubs.

::: ion.nn.Linear

::: ion.nn.Identity
