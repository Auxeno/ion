# Convolution

N-dimensional convolution. Spatial rank is inferred from `kernel_shape`, so the same class handles 1D, 2D, and 3D. Inputs are channels-last with exactly one leading batch dimension; see [Conventions](../conventions.md#spatial-layers).

::: ion.nn.Conv

::: ion.nn.ConvTranspose
