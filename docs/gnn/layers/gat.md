# GAT

Multi-head graph attention. `GATConv` is the original static-attention formulation; `GATv2Conv` is the dynamic-attention variant. Both support edge features and edge masking; see [GNN](../index.md) for the array conventions.

::: ion.gnn.GATConv

::: ion.gnn.GATv2Conv
