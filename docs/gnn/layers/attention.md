# Attention

Multi-head attention over graph neighbourhoods. `GATConv` and `GATv2Conv` use additive attention; `TransformerConv` uses scaled dot-product attention. All three support edge features and accept an optional `edge_mask`.

::: ion.gnn.GATConv

::: ion.gnn.GATv2Conv

::: ion.gnn.TransformerConv
